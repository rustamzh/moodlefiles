import { CourseFileWithSpinner } from './course-file-with-spinner';

describe('CourseFileWithSpinner', () => {
  it('should create an instance', () => {
    expect(new CourseFileWithSpinner()).toBeTruthy();
  });
});
