import { CourseWithSpinner } from './course-with-spinner';

describe('CourseWithSpinner', () => {
  it('should create an instance', () => {
    expect(new CourseWithSpinner()).toBeTruthy();
  });
});
